class AppConfig {
  /// Base URL of the FastAPI backend.
  /// Change this before building the APK to point to your server.
  static const String apiBaseUrl = 'https://cataract.digitalrepublic.space';
}
